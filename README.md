## Description
Next.js のテンプレート
 - `New Repository` を選択
 - `Repository Template` で `next-template` を選択
 - このテンプレートが新しいレポジトリにコピーされる

### 実行
- 必要なパッケージをインストール
```
yarn
```
- ローカルでサーバを起動

```bash
yarn dev
```
